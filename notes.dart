class Note {
  int id;
  String title;
  String content;

  Note({required this.id, required this.title, required this.content});
}

List<Note> sample = [
  Note(id: 0, title: "cod3ddy", content: "-make a shadow web"),
  Note(id: 0, title: "Cookies", content: "-sell cookies"),
];
